apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: prime-game-autoscaler
spec:
  minReplicas: 1
  maxReplicas: 5
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: prime-game-deployment
  targetCPUUtilizationPercentage: 60
